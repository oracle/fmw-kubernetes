# Copyright (c) 2026, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
#
{{- if eq .Values.type "TRAEFIK" }}
{{- if (eq .Values.sslType "SSL") }}
{{- if not .Values.hostName.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.wlsDomain.domainUID }}-traefik
  namespace: {{ .Release.Namespace }}
  labels:
    weblogic.resourceVersion: domain-v2
  annotations:
    kubernetes.io/ingress.class: 'traefik'
spec:
  entryPoints:
  - websecure
  routes:
  - match: PathPrefix(`/console`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-{{ .Values.wlsDomain.adminServerName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.adminServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/consolehelp`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-{{ .Values.wlsDomain.adminServerName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.adminServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/em`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-{{ .Values.wlsDomain.adminServerName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.adminServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/ws_utc`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.soaClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.soaManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/soa`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.soaClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.soaManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/integration`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.soaClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.soaManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/soa-infra`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.soaClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.soaManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/identity`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/admin`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/dms`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-{{ .Values.wlsDomain.adminServerName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.adminServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/oim`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/sysadmin`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/workflowservice`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/callbackResponseService`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/spml-xsd`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/HTTPClnt`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/reqsvc`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/iam`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/provisioning-callback`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/CertificationCallbackService`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/ucs`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/FacadeWebApp`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/OIGUI`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
  - match: PathPrefix(`/weblogic`)
    kind: Rule
    middlewares: 
    - name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
      namespace: "{{ .Release.Namespace }}"
    services:
    - name: '{{ .Values.wlsDomain.domainUID }}-cluster-{{ .Values.wlsDomain.oimClusterName | lower | replace "_" "-" }}'
      port: {{ .Values.wlsDomain.oimManagedServerPort }}
      sticky:
        cookie:
          name: sticky-cookie
          httpOnly: true
---
#Create Traefik Middleware custom resource for SSL Termination
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: "{{ .Values.wlsDomain.domainUID }}-proxy-ssl"
  namespace:  {{ .Release.Namespace }}
spec:
  headers:
    customRequestHeaders:
      X-Custom-Request-Header: ""
      X-Forwarded-For: ""
      WL-Proxy-Client-IP: ""
      WL-Proxy-SSL: ""
      X-Forwarded-Proto: "https"
      WL-Proxy-SSL: "true"
    sslRedirect: true
---
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: {{ .Release.Namespace }}
spec:
  defaultCertificate:
    secretName:  {{ .Values.wlsDomain.domainUID }}-tls-cert
{{- end }}
{{- end }}
{{- end }}
